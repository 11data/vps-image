# Environment variables for RunYourAgent VPS
# Copy this to .env and fill in the values during provisioning

# Gateway Configuration
OPENCLAW_GATEWAY_TOKEN=changeme-random-token-32chars

# PostgreSQL Password (auto-generated during provisioning)
POSTGRES_PASSWORD=changeme-random-password-64chars

# Instance ID (for Mission Control)
OPENCLAW_INSTANCE_ID=vps
